[pytest]
testpaths = tests
# The collector modules sit at the repository root and are run as scripts, so
# they are not installed as a package. Put the root on sys.path explicitly;
# relying on the working directory only works for "python -m pytest".
pythonpath = .
